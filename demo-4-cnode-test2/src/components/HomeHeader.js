import React from 'react'
import {render} from 'react-dom'

const HomeHeader=({value,onChange1})=>(
    <nav className="homeHeader">
        
        <button >分享</button>
    </nav>
)
export default HomeHeader